on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x, 23.x]

    defaults:
      run:
        working-directory: ./app  # Set default working directory for all run steps

    steps:
    - name: Checkout code
<<<<<<< HEAD
      uses: actions/checkout@v2
=======
      uses: actions/checkout@v2 # Step 1: Check out the source code from the repository.
      with:
        path: './app'
>>>>>>> a15012721ee2dc63de9cad3ffe8ce24b79381ccf

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: app/package-lock.json  # Point to lock file in app directory


    - name: Install Node.js dependencies
<<<<<<< HEAD
      run: npm ci

    - name: Run tests
      run: npm test
=======
      run: npm ci # Step 3: Perform a clean install of Node.js dependencies.


    - name: Run tests
      run: npm test # Step 4: Run tests using the specified Node.js version.

>>>>>>> a15012721ee2dc63de9cad3ffe8ce24b79381ccf
